services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: acmind
      POSTGRES_PASSWORD: acmind
      POSTGRES_DB: acmind
    ports: ["5432:5432"]
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U acmind -h localhost"]
      interval: 5s
      timeout: 3s
      retries: 5

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    # Load the whole .env into the container, exactly like a local `cargo run`
    # (DATABASE_URL, JWT, RATE_LIMIT, LLM_*, …). DATABASE_URL in .env is the
    # switch that decides which database this stack talks to:
    #   full local docker  → postgres://acmind:acmind@postgres:5432/acmind
    #   server / external  → postgres://<user>:<pass>@<host>:<port>/acmind
    env_file:
      - .env
    ports: ["8080:8080"]
    depends_on:
      postgres:
        condition: service_healthy

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    environment:
      VITE_API_BASE_URL: http://localhost:8080
    ports: ["5173:80"]
    depends_on:
      - api

volumes:
  postgres_data:
